#pragma once

#include "Property.h"

namespace coral
{
    // config to use for the engine
    // simplify the engine pipeline creation
    class EngineConfig
    {
    public:
        static void setup();
    };
}
